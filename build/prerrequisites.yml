version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.9  
    commands:
      - echo "Installing Terraform..."
      - curl -LO "https://releases.hashicorp.com/terraform/$(curl -s https://checkpoint-api.hashicorp.com/v1/check/terraform | jq -r .current_version)/terraform_$(curl -s https://checkpoint-api.hashicorp.com/v1/check/terraform | jq -r .current_version)_linux_amd64.zip"
      - unzip terraform_*_linux_amd64.zip
      - mv terraform /usr/local/bin/
      - terraform --version
      - echo "Installing Checkov..."
      - pip install checkov
      - echo "Installing jq..."
      - if command -v yum >/dev/null; then yum install -y jq; elif command -v apt-get >/dev/null; then apt-get update && apt-get install -y jq; else echo "Package manager not found. Please install jq manually."; fi

  pre_build:
    commands:
      - echo "Assuming IAM role..."
      - ROLE_ARN="arn:aws:iam::$AWS_ACCOUNT:role/TerraformDeploymentRole"  
      - CREDENTIALS=$(aws sts assume-role --role-arn $ROLE_ARN --role-session-name CodeBuildSession)
      - export AWS_ACCESS_KEY_ID=$(echo $CREDENTIALS | jq -r .Credentials.AccessKeyId)
      - export AWS_SECRET_ACCESS_KEY=$(echo $CREDENTIALS | jq -r .Credentials.SecretAccessKey)
      - export AWS_SESSION_TOKEN=$(echo $CREDENTIALS | jq -r .Credentials.SessionToken)
      - echo "Configurando credenciales de Git..."
      - git config --global credential.helper store
      - echo "https://$GITHUB_TOKEN:x-oauth-basic@github.com" > ~/.git-credentials
      - echo "Creating Checkov report folder..."
      - mkdir checkov_report
      - echo "Running Checkov for static code analysis..."
      - checkov -o junitxml --framework terraform --skip-check CKV_AWS_LOW --download-external-modules true -d ./ -s > ./checkov_report/checkov.xml
      - checkov -o json --framework terraform --download-external-modules true --skip-check CKV_AWS_LOW -d ./ -s

  build:
    commands:
      - echo "Initializing Terraform..."
      - terraform init
      - echo "Planning Terraform deployment..."
      - terraform plan -out=tfplan -var-file=environments/dev/terraform.tfvars
      - echo "Applying Terraform deployment..."
      - terraform apply -auto-approve -var-file=environments/dev/terraform.tfvars tfplan

  post_build:
    commands:
      - echo "Terraform deployment completed."
      - echo "Cleaning up..."
      - rm terraform_*_linux_amd64.zip
      - rm -rf .terraform

artifacts:
  files:
    - tfplan
    - checkov_report/checkov.xml
